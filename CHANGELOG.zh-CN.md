# 更新日志

本文件记录 Rocket-Leaf 的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循
[语义化版本](https://semver.org/lang/zh-CN/spec/v2.0.0.html)。

[English](CHANGELOG.md)

## [0.1.3] - 2026-08-06

### 新增

- 发送测试消息、消息查询两个页面的 Topic 选择器支持输入过滤，集群里有上百个 Topic 时
  不用再一条条往下翻。匹配结果按贴合程度排序：完全一致的排最前，其次是以输入内容开头
  的，最后才是包含它的。
- 两处选择器打开即可看到「最近使用」，保留最近真正发送或查询过的 10 个 Topic——通常
  就是调试某个服务时反复用到的那几个。该列表按连接分别保存，不会把别的集群的 Topic 名
  混进来；集群上已经删掉的 Topic 也不会再出现。
- 消息查询页「重试队列」「死信队列」下的消费者组选择器同样支持搜索，并有一份自己的最近
  使用列表。
- 下拉一次最多渲染 200 条匹配结果，并在底部说明还剩多少条，集群再大也能秒开。

## [0.1.2] - 2026-08-06

### 新增

- 更新检查不再依赖手动点击：启动后不久以及之后每 24 小时会向 GitHub 查询一次新版本。
  发现新版本时只提示一次，随后由侧栏「设置」上的标记接力，直到进入该页面为止，不必
  自己去翻设置才知道有更新。
- 设置 → 通用 新增「自动检查更新」开关，默认开启。不希望应用自行发起网络请求的话可以
  关掉；该请求只携带当前版本号。

### 变更

- 设置 → 关于 的「检查更新」按钮与自动检查共用同一份状态，手动检查同样会清除侧栏标记
  并更新「关于」卡片上显示的信息。

## [0.1.1] - 2026-08-06

### 新增

- 连接支持分组。分组为自由文本且可留空：输入名称即新建分组，选择已有名称即复用，
  留空则不分组。
- 连接列表按分组归类，分组标题可折叠，命名分组在前、未分组在后。一个分组都没有时，
  列表保持原来的平铺样式。
- 连接搜索会匹配分组名称，并在搜索期间临时展开已折叠的分组，避免命中项被标题挡住。

### 变更

- 连接的「环境」字段（生产 / 测试 / 开发）由「分组」取代。原来的预设只影响一个徽章
  的颜色，且无法留空，导致每个连接都被迫带上一个未必有意义的标签。

### 移除

- 加载连接配置时不再读取原有的环境值。已保存的连接会以「未分组」状态打开，不会被
  归入以旧预设命名的分组。连接的其余配置不受影响。

## [0.1.0] - 2026-08-05

### 新增

- 基于 Wails 3 重构的桌面客户端首个版本，提供 macOS、Windows、Linux 的 amd64 与
  arm64 安装包。

### 变更

- 重构后的应用版本号从 0.1.0 重新开始。本次发布不是此前 1.x 系列的延续，也不提供
  从 1.x 的升级路径。

[0.1.3]: https://github.com/amigoer/rocket-leaf/compare/v0.1.2...v0.1.3
[0.1.2]: https://github.com/amigoer/rocket-leaf/compare/v0.1.1...v0.1.2
[0.1.1]: https://github.com/amigoer/rocket-leaf/compare/v0.1.0...v0.1.1
[0.1.0]: https://github.com/amigoer/rocket-leaf/releases/tag/v0.1.0
