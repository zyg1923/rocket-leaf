# 打包与部署

这个仓库提交的是源码。`node_modules`、前端构建结果和安装包不进 Git，它们都可以在本机重新生成。

## 环境

- Go 1.25 或更高
- Node.js 20 或更高、npm
- [Wails 3 CLI](https://v3.wails.io)
- Windows 安装包还需要 NSIS（`makensis` 在 PATH 里）

安装 CLI：

```powershell
go install github.com/wailsapp/wails/v3/cmd/wails3@latest
```

## 安装依赖并开发运行

```powershell
npm install
npm --prefix frontend install
npm run dev
```

## 打包

```powershell
npm run package
```

Windows 上这会先编译程序，再调用 `wails3 generate webview2bootstrapper` 下载 WebView2 引导安装程序，然后用 NSIS 生成安装包。

也可以只编译、不打安装包：

```powershell
npm run build
```

## 部署

把生成的 Windows 安装包拷到目标机器安装。对方不需要安装 Go 或 Node.js。安装包会带上 WebView2 引导程序；如果系统里还没有 WebView2 运行时，安装过程会处理。

## 未入库的文件

| 路径 | 是什么 | 没有它会怎样 | 怎么补 |
| --- | --- | --- | --- |
| `node_modules\`、`frontend\node_modules\` | npm 依赖 | 不能开发，也不能打包 | `npm install`，以及 `npm --prefix frontend install` |
| `frontend\dist\` | 前端编译结果 | 没有现成的页面包 | `npm run build:frontend` 或 `npm run package` |
| `bin\` | Go / Wails 编译出的程序和安装包 | 没有现成的 exe | `npm run build` 或 `npm run package` |
| `build\windows\nsis\MicrosoftEdgeWebview2Setup.exe` | 打包时下载的 WebView2 引导程序 | 不影响源码；打安装包时会重新下载 | `npm run package` |
| `*.syso` | Windows 图标和清单的生成文件 | 打包时会重新生成 | `npm run package` |

`build\windows\nsis\project.nsi` 是安装脚本源码，已经在 Git 里。
